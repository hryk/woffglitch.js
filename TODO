 * フォントテーブルのオフセットはlength % 4で判断する。offset % 4だとたまたま合うケースがある。
   現状のget_table_offsetはpadding付きの値を返すかどうかをフラグで判断しているが、実データから計算しないと累積だから正しい値が出ない。
 * 最初に全部populateする方が楽。あとデータをキャッシュ(テーブルのデータごとキャッシュ)して持っといた方が良さげ。
